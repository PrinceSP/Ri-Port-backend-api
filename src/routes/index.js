const authRoute= require('./auth')
const userRoute= require('./users')
const postRoute= require('./posts')
const feedbackRoute= require('./feedback')

module.exports = {authRoute,userRoute,postRoute,feedbackRoute}
